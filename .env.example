# 复制为 .env.development.local 或在本机 .env 中按需填写（勿提交含密钥的文件）
#
# 本地 API：可写下面地址直连后端；日常开发也可留空，使用 vite.config 里 /api 代理到 127.0.0.1:8000
VITE_API_BASE_URL=http://127.0.0.1:8000
#
# 线上构建（GitHub Pages）：不要用本文件，在仓库 Settings → Secrets → Actions
# 设置 VITE_API_BASE_URL（须 https://，如 https://api.iww.pub）；本地示例见 .env.production.example
