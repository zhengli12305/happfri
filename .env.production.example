# 构建生产前端：复制为 .env.production 后执行 npm run build
# 与 Nginx 同域反代 /api 到后端时，留空即可（请求走 /api/...）
VITE_API_BASE_URL=
VITE_PUBLIC_BASE=/

# 若前端与 API 不同源（例如静态在 OSS/CDN，API 在子域），改为完整 API 根地址，不要末尾斜杠：
# VITE_API_BASE_URL=https://api.yourdomain.com
